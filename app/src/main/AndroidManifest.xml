<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elder.cervejeirossa">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".app.beerList.BeerListActivity"
            android:label="@string/list_beers"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.beerDetail.BeerDetailActivity"
            android:label="@string/title_activity_beer_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".app.favoriteList.FavoriteListActivity"
            android:label="@string/favorite_beers" />
        <activity android:name=".app.beerDetail.BeerPictureActivity"
            android:label="@string/beer_picture"></activity>
    </application>

</manifest>